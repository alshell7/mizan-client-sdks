# Security policy

Report vulnerabilities privately through a GitHub security advisory for `alshell7/mizan-client-sdks`.
Do not include live Mizan tokens, customer identifiers, or payment data. SDK credentials are server-side
secrets and must never be embedded in browser or mobile builds.

